% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab2fig.R
\name{t2f_internal}
\alias{t2f_internal}
\title{Internal implementation for t2f}
\usage{
t2f_internal(
  df,
  filename = NULL,
  sub_dir = "figures",
  scolor = NULL,
  verbose = FALSE,
  extra_packages = NULL,
  document_class = NULL,
  caption = NULL,
  caption_short = NULL,
  label = NULL,
  align = NULL,
  longtable = FALSE,
  crop = TRUE,
  crop_margin = 10,
  theme = NULL,
  footnote = NULL,
  header_above = NULL,
  collapse_rows = NULL
)
}
\arguments{
\item{df}{A dataframe to be converted to a LaTeX table.}

\item{filename}{A character string. The base name of the output files
(without extensions).}

\item{sub_dir}{A character string. The subdirectory where output files will
be stored. Defaults to "figures".}

\item{scolor}{A LaTeX color name for alternating row shading in the table
(e.g., "blue!10"). Overrides theme setting if provided.}

\item{verbose}{Logical. If TRUE, prints progress messages.}

\item{extra_packages}{A list of LaTeX package specifications. Can include
helper functions like geometry(), babel(), fontspec() or raw LaTeX
strings. Combined with theme packages if a theme is active.}

\item{document_class}{LaTeX document class to use. Defaults to "article".
Overrides theme setting if provided.}

\item{caption}{A character string. LaTeX caption for the table. Defaults to
NULL (no caption).}

\item{caption_short}{A character string. Short caption for List of Tables.
Defaults to NULL (uses full caption).}

\item{label}{A character string. LaTeX label for cross-referencing (e.g.,
"tab:mytable"). Defaults to NULL (no label).}

\item{align}{Column alignment specification. Can be NULL (auto-detect),
a single character ("l", "c", "r") applied to all columns, a character
vector with one alignment per column, or a list that may include
t2f_siunitx objects for decimal alignment.}

\item{longtable}{Logical. If TRUE, uses longtable package for tables
spanning multiple pages. Defaults to FALSE.}

\item{crop}{Logical. If TRUE (default), crops the PDF to remove margins.}

\item{crop_margin}{Numeric. Margin size in points for cropped PDF. Can be a
single value (applied to all sides) or a vector of 4 values (left, top,
right, bottom). Defaults to 10.}

\item{theme}{A t2f_theme object or character string naming a built-in theme
("minimal", "apa", "nature", "nejm"). Theme settings are used as defaults
but can be overridden by explicit arguments.}

\item{footnote}{A t2f_footnote object created by t2f_footnote(). Specifies
table footnotes with various notation styles.}

\item{header_above}{A t2f_header object or list of them, created by
t2f_header_above(). Specifies spanning column headers.}

\item{collapse_rows}{A t2f_collapse object created by t2f_collapse_rows().
Specifies columns to merge into multi-row cells.}
}
\value{
Invisibly returns the path to the cropped PDF file (or full PDF if
crop=FALSE).
}
\description{
Internal function that performs the actual table generation.
This is called by the S3 methods defined in s3-methods.R.
}
\keyword{internal}
