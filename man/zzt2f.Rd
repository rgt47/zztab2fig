% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzt2f.R
\name{zzt2f}
\alias{zzt2f}
\title{Typst Backend for Table-to-Figure Conversion}
\usage{
zzt2f(
  x,
  filename = NULL,
  sub_dir = get_default_figures_dir(),
  verbose = FALSE,
  caption = NULL,
  align = NULL,
  theme = NULL,
  scolor = NULL,
  footnote = NULL,
  header_above = NULL,
  format = c("pdf", "png", "svg"),
  dpi = 300L,
  ...
)
}
\arguments{
\item{x}{A data frame, matrix, or table object to render.}

\item{filename}{Character string. Base name for output file (without
extension). Defaults to the deparsed name of \code{x}.}

\item{sub_dir}{Character string. Output directory. Defaults to
"analysis/figures" in zzcollab projects, "figures" otherwise.}

\item{verbose}{Logical. Print progress messages. Default FALSE.}

\item{caption}{Character string or NULL. Table caption.}

\item{align}{Character vector or NULL. Column alignments ("l", "c", "r").
Length 1 (applied to all columns) or one per column. NULL for auto-detect.}

\item{theme}{Character string, t2f_theme object, or NULL. Theme name
(e.g., "nejm", "apa") or custom theme. Uses the existing t2f theme system.}

\item{scolor}{Character string or NULL. LaTeX color spec for row striping,
translated to hex. Overrides theme setting.}

\item{footnote}{A t2f_footnote object or NULL. Table footnotes.}

\item{header_above}{A t2f_header object, list of t2f_header objects, or
NULL. Spanning column headers.}

\item{format}{Character string. Output format: "pdf" (default), "png",
or "svg".}

\item{dpi}{Integer. PNG resolution in dots per inch. Default 300.}

\item{...}{Additional arguments passed to \code{tinytable::tt()}.}
}
\value{
Invisibly returns the path to the output file.
}
\description{
Convert data frames to publication-quality table images using
tinytable and the Typst CLI. This provides a lightweight alternative to
the LaTeX-based \code{\link{t2f}} pipeline, requiring only Typst (~40 MB
binary) instead of a full LaTeX distribution.

Generates PDF, PNG, or SVG table images from data frames using
the tinytable package and Typst CLI. Typst auto-sizes pages, so no
cropping step is needed.
}
\details{
The pipeline:
\enumerate{
\item Validate inputs and coerce to data.frame
\item Resolve theme via existing t2f theme system, translate to Typst
\item Build tinytable object with \code{tt()} / \code{style_tt()} /
\code{group_tt()}
\item Save to \code{.typ} via \code{save_tt()}
\item Compile via \code{typst compile}
}

Parameters from \code{t2f()} that are dropped (LaTeX-specific):
\code{extra_packages}, \code{document_class}, \code{caption_short},
\code{label}, \code{longtable}, \code{crop}, \code{crop_margin},
\code{collapse_rows}.
}
\examples{
\dontrun{
zzt2f(mtcars[1:6, 1:4], filename = "mtcars_sample")
zzt2f(mtcars[1:6, 1:4], filename = "nejm_table",
      theme = "nejm", format = "png")
}

}
