% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rms-methods.R
\name{t2f.cph}
\alias{t2f.cph}
\title{t2f method for rms Cox proportional hazards models}
\usage{
\method{t2f}{cph}(
  x,
  digits = 3,
  exponentiate = TRUE,
  conf.int = TRUE,
  conf.level = 0.95,
  output = c("coef", "anova"),
  ...
)
}
\arguments{
\item{x}{A cph object from the rms package.}

\item{digits}{Number of decimal places.}

\item{exponentiate}{Logical. If TRUE, exponentiate coefficients to get
hazard ratios.}

\item{conf.int}{Logical. Include confidence intervals.}

\item{conf.level}{Confidence level for intervals.}

\item{output}{Character. Type of output: "coef" for coefficient table,
"anova" for ANOVA-style chunk tests.}

\item{...}{Additional arguments passed to t2f().}
}
\value{
Invisibly returns the path to the generated PDF.
}
\description{
t2f method for rms Cox proportional hazards models
}
\examples{
\dontrun{
library(rms)
library(survival)
dd <- datadist(lung)
options(datadist = "dd")
model <- cph(Surv(time, status) ~ age + rcs(meal.cal, 4) + sex, data = lung)
t2f(model, exponentiate = TRUE, filename = "cox_hazard_ratios")
t2f(model, output = "anova", filename = "cox_anova")
}

}
