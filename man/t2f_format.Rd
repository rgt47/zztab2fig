% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formatting.R
\name{t2f_format}
\alias{t2f_format}
\title{Create a formatting specification}
\usage{
t2f_format(
  rows = NULL,
  cols = NULL,
  bold = FALSE,
  italic = FALSE,
  color = NULL,
  background = NULL,
  condition = NULL
)
}
\arguments{
\item{rows}{Integer vector. Row indices to format (1-based, excluding
header). NULL to apply to all rows.}

\item{cols}{Integer vector or character vector. Column indices or names to
format. NULL to apply to all columns.}

\item{bold}{Logical. If TRUE, apply bold formatting.}

\item{italic}{Logical. If TRUE, apply italic formatting.}

\item{color}{Character. LaTeX color name for text (e.g., "red", "blue").}

\item{background}{Character. LaTeX color name for cell background.}

\item{condition}{A function that takes a cell value and returns TRUE/FALSE.
Formatting is applied only to cells where condition returns TRUE.}
}
\value{
A t2f_format object (list with class "t2f_format").
}
\description{
Define formatting rules for specific cells in a table.
Formatting can target specific rows, columns, or both, and can include
bold, italic, colors, or conditional highlighting.
}
\examples{
\dontrun{
# Bold specific cells
t2f_format(rows = 1:3, cols = 1, bold = TRUE)

# Highlight a column
t2f_format(cols = "p_value", background = "yellow!30")

# Conditional formatting
t2f_format(cols = "p_value",
           condition = function(x) as.numeric(x) < 0.05,
           bold = TRUE, color = "red")
}

}
