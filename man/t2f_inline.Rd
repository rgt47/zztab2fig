% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inline.R
\name{t2f_inline}
\alias{t2f_inline}
\title{Inline Table for R Markdown Documents}
\usage{
t2f_inline(
  x,
  width = NULL,
  height = NULL,
  align = c("center", "left", "right"),
  filename = NULL,
  format = c("auto", "pdf", "png"),
  dpi = 150,
  sub_dir = NULL,
  caption = NULL,
  caption_short = NULL,
  label = NULL,
  caption_position = c("above", "below"),
  frame = FALSE,
  frame_color = "black",
  frame_width = "0.4pt",
  background = NULL,
  inner_sep = "2pt",
  ...
)
}
\arguments{
\item{x}{An object to convert to a table (data.frame, lm, glm, etc.).}

\item{width}{Figure width. Use LaTeX units for PDF ("2in", "5cm",
"0.5\\textwidth") or CSS/pixels for HTML.}

\item{height}{Optional figure height. If NULL, aspect ratio is preserved.}

\item{align}{Alignment: "left", "center", or "right".}

\item{filename}{Optional filename. If NULL, a temp file is used.}

\item{format}{Output format: "auto" (detect from knitr), "pdf", or "png".}

\item{dpi}{Resolution for PNG output. Default 150.}

\item{sub_dir}{Directory for output files. If NULL (default), uses tempdir().}

\item{caption}{Table caption. Uses \\captionof for non-float placement.
Defaults to NULL (no caption).}

\item{caption_short}{Short caption for List of Tables. Defaults to NULL.}

\item{label}{LaTeX label for cross-referencing (e.g., "tab:model").
Defaults to NULL (no label).}

\item{caption_position}{Position of caption: "above" (default, standard for
tables) or "below".}

\item{frame}{Logical. If TRUE, draw a border around the table. Default FALSE.}

\item{frame_color}{Color for the frame border. Uses xcolor syntax (e.g.,
"black", "blue!50"). Default "black".}

\item{frame_width}{Line width for the frame. Default "0.4pt".}

\item{background}{Background color for the table container. Uses xcolor
syntax (e.g., "gray!10", "yellow!5"). Default NULL (no background).}

\item{inner_sep}{Padding between content and frame. Default "2pt".}

\item{...}{Additional arguments passed to t2f().}
}
\value{
For knitr, returns the result of knitr::asis_output() with LaTeX
code. When called outside knitr, returns the file path invisibly.
}
\description{
Generate a table and include it inline in an R Markdown
document. Automatically handles PDF vs HTML output and provides
control over size, alignment, captioning, and visual styling without
using floats.
}
\details{
This function streamlines the workflow for including tables in R Markdown:
\enumerate{
\item Generates the table PDF via t2f() (without embedded caption)
\item For HTML output, converts to PNG
\item Wraps in LaTeX with alignment and caption using \\captionof
}

The table is placed exactly where the code chunk appears (no float).
Captions use \\captionof{table}{...} which requires the LaTeX \code{caption}
package. Add to your R Markdown YAML header:

\if{html}{\out{<div class="sourceCode">}}\preformatted{header-includes:
  - \\\\usepackage\{caption\}
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
# In R Markdown chunk:
model <- lm(mpg ~ cyl + hp, data = mtcars)
t2f_inline(model, width = "2in", align = "left")

# With caption and label for cross-referencing:
t2f_inline(model,
           width = "3in",
           caption = "Linear model coefficients",
           label = "tab:model")

# Caption below the table:
t2f_inline(model,
           width = "3in",
           caption = "Results",
           caption_position = "below")

# With frame border:
t2f_inline(model,
           width = "3in",
           frame = TRUE,
           frame_color = "gray",
           frame_width = "0.5pt")

# With background color:
t2f_inline(model,
           width = "3in",
           background = "gray!5")

# With both frame and background:
t2f_inline(model,
           width = "3in",
           frame = TRUE,
           frame_color = "blue!50",
           background = "blue!5",
           inner_sep = "4pt")

# With explicit format:
t2f_inline(mtcars[1:5,], width = "4in", format = "png", dpi = 300)
}

}
