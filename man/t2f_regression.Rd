% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-methods.R
\name{t2f_regression}
\alias{t2f_regression}
\title{Create side-by-side regression comparison table}
\usage{
t2f_regression(
  ...,
  include = c("estimate", "std.error"),
  stars = c(0.05, 0.01, 0.001),
  digits = 3,
  se_in_parens = TRUE,
  filename = "regression_table",
  sub_dir = "output",
  t2f_args = list()
)
}
\arguments{
\item{...}{Named lm or glm objects to compare.}

\item{include}{Character vector of statistics to include per model.}

\item{stars}{Logical or numeric vector. If TRUE, use default significance
thresholds. If numeric, specifies p-value cutoffs for stars.}

\item{digits}{Number of decimal places.}

\item{se_in_parens}{Logical. Show standard errors in parentheses below
estimates.}

\item{filename}{Base name for output files.}

\item{sub_dir}{Output directory for generated files.}

\item{t2f_args}{List of additional arguments passed to t2f().}
}
\value{
Invisibly returns the path to the generated PDF.
}
\description{
Generate a publication-ready table comparing multiple
regression models side-by-side.
}
\examples{
\dontrun{
m1 <- lm(mpg ~ cyl, data = mtcars)
m2 <- lm(mpg ~ cyl + hp, data = mtcars)
m3 <- lm(mpg ~ cyl + hp + wt, data = mtcars)
t2f_regression(Model1 = m1, Model2 = m2, Model3 = m3)
}

}
