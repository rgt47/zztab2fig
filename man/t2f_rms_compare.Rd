% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rms-methods.R
\name{t2f_rms_compare}
\alias{t2f_rms_compare}
\title{Create side-by-side comparison table for rms models}
\usage{
t2f_rms_compare(
  ...,
  digits = 3,
  exponentiate = FALSE,
  stars = c(0.05, 0.01, 0.001),
  filename = "rms_comparison",
  sub_dir = get_default_figures_dir(),
  t2f_args = list()
)
}
\arguments{
\item{...}{Named rms model objects to compare (ols, lrm, cph, etc.).}

\item{digits}{Number of decimal places.}

\item{exponentiate}{Logical. Exponentiate coefficients where appropriate.}

\item{stars}{Logical or numeric vector for significance stars.}

\item{filename}{Base name for output files.}

\item{sub_dir}{Output directory.}

\item{t2f_args}{List of additional arguments passed to t2f().}
}
\value{
Invisibly returns the path to the generated PDF.
}
\description{
Generate a publication-ready table comparing multiple rms
regression models side-by-side, similar to t2f_regression() but
optimized for rms objects.
}
\examples{
\dontrun{
library(rms)
dd <- datadist(mtcars)
options(datadist = "dd")
m1 <- ols(mpg ~ wt, data = mtcars)
m2 <- ols(mpg ~ wt + cyl, data = mtcars)
m3 <- ols(mpg ~ wt + cyl + hp, data = mtcars)
t2f_rms_compare(Model1 = m1, Model2 = m2, Model3 = m3)
}

}
