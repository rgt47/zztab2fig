% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced-features.R
\name{t2f_siunitx}
\alias{t2f_siunitx}
\title{Create siunitx column specification for decimal alignment}
\usage{
t2f_siunitx(
  table_format = "3.2",
  round_mode = c("none", "places", "figures"),
  round_precision = NULL,
  detect_weight = TRUE,
  group_separator = NULL
)
}
\arguments{
\item{table_format}{Format string for siunitx table-format option. Specifies
the number of integer and decimal places (e.g., "3.2" for up to 999.99).}

\item{round_mode}{Rounding mode: "places", "figures", or "none".}

\item{round_precision}{Number of decimal places or significant figures.}

\item{detect_weight}{Logical. Detect and preserve bold text.}

\item{group_separator}{Thousands separator (e.g., "," or " ").}
}
\value{
A t2f_siunitx object containing the column specification and
required LaTeX packages.
}
\description{
Generate a siunitx S column specification for decimal-aligned
numeric columns. This provides proper alignment on the decimal point.
}
\details{
The siunitx package provides sophisticated number formatting including
decimal alignment. This function creates a custom column type that can be
used in the align parameter.

Note: When using siunitx columns, non-numeric content (like header text)
must be wrapped in braces \code{\{text\}}.
}
\examples{
\dontrun{
# Create decimal-aligned column spec
dec_align <- t2f_siunitx(table_format = "2.3")

# Apply to specific columns using align parameter
t2f(df, align = c("l", dec_align, dec_align, "r"),
    extra_packages = attr(dec_align, "packages"))
}

}
