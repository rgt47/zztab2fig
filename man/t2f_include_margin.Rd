% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latex-include.R
\name{t2f_include_margin}
\alias{t2f_include_margin}
\title{Include a t2f table in the margin}
\usage{
t2f_include_margin(
  path,
  caption = NULL,
  label = NULL,
  width = "\\\\marginparwidth",
  offset = "0pt",
  method = c("sidenotes", "marginpar", "tufte", "marginnote"),
  cat = TRUE
)
}
\arguments{
\item{path}{Path to the PDF file.}

\item{caption}{Optional caption.}

\item{label}{Optional label.}

\item{width}{Width of graphic. Default "\\marginparwidth".}

\item{offset}{Vertical offset (e.g., "-2em", "0pt"). Default "0pt".}

\item{method}{Method for margin placement: "sidenotes", "marginpar",
"tufte", or "marginnote".}

\item{cat}{Logical. If TRUE (default), prints via cat().}
}
\value{
LaTeX code (invisibly if cat=TRUE).
}
\description{
Generate LaTeX code to place a table in the right margin.
Multiple methods supported depending on document class.
}
\details{
Methods and their requirements:
\itemize{
\item \strong{sidenotes}: Uses \code{marginfigure} environment. Requires
\verb{\\\\usepackage\{sidenotes\}}. Best for standard document classes.
\item \strong{marginpar}: Basic LaTeX \verb{\\\\marginpar\{\}}. No extra packages needed but
limited functionality (no captions).
\item \strong{tufte}: Uses \code{marginfigure} from tufte-latex classes. Only works with
tufte-book or tufte-handout document classes.
\item \strong{marginnote}: Uses \verb{\\\\marginnote\{\}} from marginnote package. Better
positioning than marginpar. Requires \verb{\\\\usepackage\{marginnote\}}.
}
}
\examples{
\dontrun{
# With sidenotes package (recommended)
t2f_include_margin("tables/summary",
                   caption = "Summary statistics",
                   method = "sidenotes")

# Simple marginpar (no packages needed)
t2f_include_margin("tables/summary", method = "marginpar")

# For tufte document classes
t2f_include_margin("tables/summary",
                   caption = "Summary",
                   method = "tufte")
}

}
