% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rms-methods.R
\name{t2f.orm}
\alias{t2f.orm}
\title{t2f method for rms ordinal regression models}
\usage{
\method{t2f}{orm}(
  x,
  digits = 3,
  exponentiate = TRUE,
  conf.int = TRUE,
  conf.level = 0.95,
  output = c("coef", "anova"),
  intercepts = FALSE,
  ...
)
}
\arguments{
\item{x}{An orm object from the rms package.}

\item{digits}{Number of decimal places.}

\item{exponentiate}{Logical. If TRUE, exponentiate coefficients to get
odds ratios.}

\item{conf.int}{Logical. Include confidence intervals.}

\item{conf.level}{Confidence level for intervals.}

\item{output}{Character. Type of output: "coef" for coefficient table,
"anova" for ANOVA-style chunk tests.}

\item{intercepts}{Logical. If TRUE, include intercept terms in coefficient
table. Default FALSE since ordinal models often have many intercepts.}

\item{...}{Additional arguments passed to t2f().}
}
\value{
Invisibly returns the path to the generated PDF.
}
\description{
t2f method for rms ordinal regression models
}
\examples{
\dontrun{
library(rms)
dd <- datadist(mtcars)
options(datadist = "dd")
mtcars$gear_ord <- ordered(mtcars$gear)
model <- orm(gear_ord ~ mpg + wt, data = mtcars)
t2f(model, exponentiate = TRUE, filename = "orm_odds_ratios")
}

}
